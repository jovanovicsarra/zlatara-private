name: Add Triade product

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/add-triade.yml'
      - '.github/scripts/add_triade.py'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: python .github/scripts/add_triade.py
      - run: |
          rm -f .github/workflows/add-triade.yml .github/scripts/add_triade.py
          git config user.name "Zlatara site updater"
          git config user.email "actions@github.com"
          git add index.html .github/workflows/add-triade.yml .github/scripts/add_triade.py
          if git diff --cached --quiet; then
            echo "No changes"
            exit 0
          fi
          git commit -m "Dodaj Ogrlicu Triade u katalog"
          git push origin main
